// Class -05 Operator
#include <stdio.h>

int main() {
    // Compound Assignment Operator : += , -= , *=, /= , %= 
    // int a=10;
    // printf("%d\n",a);
    //  a+=2; //a=a+1  // update 
    // printf("%d",a);
    
    // // b=15 , b er value 2 komaye b output hishebe dekhate 
    // int b=15;
    // b-=2;
    // printf("%d\n",b);
    
    
    // int c=12;
    // c*=3;
    // printf("%d",c);
    
    //conditional = condition jeta puron kore samner kaj korte parbo amra. 
    //if(condition){work;}
    int age;
    scanf("%d",&age);
    if(age>18)
    {
        printf("can apply for driving licence\n");
    }
    else if(age>90)
    {
        printf("too old for licence");
    }
    else
    {
        printf("can't apply for driving licence");
    }

    

    return 0;
}
